ALIGNMENT_SERVICE_PORT=8002
# Must match alignServiceToken (ALIGN_SERVICE_TOKEN in apps/web/.env) — this is the server, that
# one is the client. If unset, the service stays usable for local dev (warns once at startup) but
# every real deploy must set this via a secret.
ALIGNMENT_SERVICE_TOKEN=
ALIGN_LANGUAGE_CODE=en

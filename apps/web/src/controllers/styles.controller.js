function createStylesController(styles){return{list(req,res){res.json({styles:styles.list()});},references(req,res){const id=styles.sanitize(req.params.styleId);if(!styles.find(id))return res.status(404).json({error:'Unknown style'});res.json({styleId:id,references:styles.references(id)});},upload(req,res){const id=styles.sanitize(req.params.styleId);res.json({ok:true,styleId:id,references:styles.upload(id,req.query.type||req.body.type,req.files)});},remove(req,res){const id=styles.sanitize(req.params.styleId);res.json({ok:true,styleId:id,references:styles.remove(id,req.body.type,req.body.fileName)});}};}
module.exports={createStylesController};

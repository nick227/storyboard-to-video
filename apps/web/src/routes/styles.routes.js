const express=require('express');const {styleAdmin}=require('../middleware/style-admin');const {asyncRoute}=require('./helpers');function stylesRoutes({controller,upload}){const router=express.Router();router.get('/',controller.list);router.get('/:styleId/references',controller.references);router.post('/:styleId/references/upload',styleAdmin,upload.array('files',8),asyncRoute(controller.upload));router.delete('/:styleId/references',styleAdmin,asyncRoute(controller.remove));return router;}module.exports={stylesRoutes};

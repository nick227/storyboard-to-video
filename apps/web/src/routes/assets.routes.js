const express=require('express');const {asyncRoute}=require('./helpers');function assetsRoutes(controller){const router=express.Router();router.get('/projects/:projectId/assets/:type/:fileName',asyncRoute(controller.project));router.get('/style-references/:styleId/:type/:fileName',asyncRoute(controller.style));return router;}module.exports={assetsRoutes};

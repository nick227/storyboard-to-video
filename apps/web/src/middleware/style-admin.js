const {AppError}=require('../errors');function styleAdmin(req,res,next){const configured=String(process.env.ADMIN_OWNER_IDS||'').split(',').map(x=>x.trim()).filter(Boolean),admins=configured.length?configured:(process.env.NODE_ENV==='production'?[]:['local-user']);return admins.includes(req.user.id)?next():next(new AppError('FORBIDDEN','Style reference administration is not permitted',{status:403}));}module.exports={styleAdmin};

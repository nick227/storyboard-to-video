{
  "name": "storyboard-image-poc",
  "version": "0.2.0",
  "private": true,
  "description": "Storyboard prompt and image generation POC",
  "main": "server.js",
  "scripts": {
    "start": "nodemon --watch server.js --watch src --watch public --watch styles --watch style-references --ext js,json,html,css,md,png --exec node server.js",
    "test": "node --test test/",
    "check": "node --check server.js && node --check public/app.js && find src public/modules -name '*.js' -print0 | xargs -0 -n1 node --check",
    "setup:piper": "node scripts/setup-piper.js"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "text2wav": "^0.0.14",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "supertest": "^7.2.2"
  }
}
